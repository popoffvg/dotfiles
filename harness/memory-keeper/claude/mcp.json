{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/../server/index.ts"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}/../server",
      "env": {
        "WORK_CWD": "${CWD}",
        "PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
