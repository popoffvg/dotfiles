{"id": "rename-app-directory", "task": "Let's rename md-comment to line-comment everywhere — the app dir, the crate, the extension, the commands.", "pick": "searchable-names", "note": "Real, from the 2026-08-31 session that renamed harness/apps/md-comment. A whole-repo rename is the § Align language rule (rename in the same commit) plus the file-after-its-concept rule. Nothing here is a diff under review."}
{"id": "helpers-file", "task": "Add a helpers.ts under the client module for the bits both callers need.", "pick": "searchable-names", "note": "The file-after-a-role rule. The trigger word is a filename, not an identifier — a description that only names symbols misses it."}
{"id": "metric-built-from-parts", "task": "Emit the counter with the name built from the kind: \"pl_\" + kind + \"_total\".", "pick": "searchable-names", "note": "§ Name the literal. No one asks about naming here; the task reads as ordinary instrumentation."}
{"id": "doc-line-new-export", "task": "Write the doc comment for the new exported SessionExpiryChecker type.", "pick": "searchable-names", "note": "The doc line is part of where the search lands. Boundary against CODE_STYLE § Package the fact, which owns the prose — but the skill offered here is the one that says the line must exist and what fact it carries."}
{"id": "what-should-i-call-it", "task": "What should I call the function that loads a user and bumps its last-seen timestamp?", "pick": "searchable-names", "note": "The plainest positive. If the description misses this one, nothing else matters."}
{"id": "align-terminology", "task": "The spec says \"order\" and the code says \"purchase record\". Align the terminology.", "pick": "searchable-names", "note": "§ Align language, domain term over technical term. Overlaps the loose `terms` skill; inside a codebase this is the code rule."}
{"id": "where-does-module-live", "task": "I'm adding federated bucket support to the client. Where should the new file go and what do I call it?", "pick": "searchable-names", "note": "The base-name-plus-suffix rule (client -> client_federated). Two questions, one skill."}
{"id": "log-prefix-shared", "task": "Every log line in this package starts with \"%s: failed\" and I can't grep back to the source. Fix them.", "pick": "searchable-names", "note": "The unique-literal-prefix rule. The word \"name\" never appears in the task."}
{"id": "two-int-ids", "task": "transfer(ownerID, orgID) takes two ints. Someone swapped them in a call site last week.", "pick": "searchable-names", "note": "§ Put the domain concept in a type. Reads as a bug report; the fix is a named type."}
{"id": "copy-validator", "task": "Copy validateRunnerConfig into the worker package so it doesn't have to import the runner.", "pick": "searchable-names", "note": "§ Keep one definition site per symbol. The task asks for the violation directly."}
{"id": "review-the-naming", "task": "Review the naming in this diff before I approve it.", "pick": "pedant", "note": "The plainest pedant case — a finished diff, a reviewer, no new name being chosen."}
{"id": "pedant-bare-word", "task": "pedant", "pick": "pedant", "note": "The bare invocation word. It is in pedant's description and must not be in the other's."}
{"id": "are-these-names-clear", "task": "Are these names clear? cfg, res, acc, notReady.", "pick": "pedant", "note": "The smell table over declared names. Both descriptions talk about clear names; only pedant reviews an existing set."}
{"id": "check-names-in-pr", "task": "Check the names in PR 221 and tell me which ones hide a bug.", "pick": "pedant", "note": "\"the bug a name hides\" is pedant's stated payoff. Guards against searchable-names swallowing all review work."}
{"id": "rename-what-review-flagged", "task": "The reviewer flagged `data` as meaningless. Pick the real name and apply it.", "pick": "searchable-names", "note": "THE HARD CASE, and the whole reason the split needs grading. It starts in review, so pedant looks right — but pedant proposes and never renames, and choosing the replacement is authoring. If this case flips, the two descriptions have not drawn the line."}
{"id": "commit-message", "task": "Write the commit message for this change.", "pick": "none", "note": "HARD NEGATIVE. § Align language names commits as a place one term per concept holds, so the description brushes against this. The `commit-message` skill owns it; neither naming skill may claim it."}
{"id": "tighten-comment-prose", "task": "This module docstring is 90 words and rambles. Tighten it.", "pick": "none", "note": "HARD NEGATIVE against `doc-line-new-export`. Comment prose stayed in CODE_STYLE § Package the fact. The pair guards the seam in both directions."}
{"id": "switch-on-column-id", "task": "The renderer has an `if id == \"status\"` branch. Move that fact into the column table.", "pick": "none", "note": "HARD NEGATIVE. CODE_STYLE § Declarative table vs imperative reader. A column id is an identifier, so the description's \"field name\" clause can pull this in wrongly."}
{"id": "run-the-tests", "task": "Run the test suite and fix whatever breaks.", "pick": "none", "note": "Plain negative. Any description firing here fires on everything."}
{"id": "profile-slow-loop", "task": "This loop takes 8 seconds on a 10k-row input. Profile it and make it faster.", "pick": "none", "note": "Plain negative — code work with no naming decision in it."}
