{"id": "signature-diff", "intent": "the minter's new signature, for the Surface block", "candidate": "```diff\n-func mintTokens(userID string) (string, error)\n+func mintTokens(userID string) (TokenPair, error)\n```", "half": "human", "form": "keep", "note": "The canonical increment: one signature, nothing else. The replacement body is the implementer's."}
{"id": "new-struct-fields", "intent": "the request and pair types, for the Surface block", "candidate": "```diff\n+type RefreshRequest struct {\n+\tToken string `json:\"token\"`\n+}\n+\n+type TokenPair struct {\n+\tAccess  string `json:\"access\"`\n+\tRefresh string `json:\"refresh\"`\n+}\n```", "half": "human", "form": "keep", "note": "Fields are surface \u2014 a caller sees every one of them. All-`+`, nothing elided."}
{"id": "config-defaults-diff", "intent": "the new retry settings, for the Surface block", "candidate": "```diff\n auth:\n   tokenTTL: 15m\n+  refreshRetries: 3\n+  refreshBackoff: 250ms\n```", "half": "human", "form": "keep", "note": "Settings with their real values are surface, explicitly. The trap: looks like data, is contract."}
{"id": "enum-values-diff", "intent": "the rotation outcome enum, for the Surface block", "candidate": "```diff\n+const (\n+\tRotationOK       RotationResult = \"ok\"\n+\tRotationReplayed RotationResult = \"replayed\"\n+\tRotationExpired  RotationResult = \"expired\"\n+)\n```", "half": "human", "form": "keep", "note": "Hard keep case: a block of literal values that IS surface, because consumers branch on the strings."}
{"id": "surface-compile-floor", "intent": "the whole gateway interface, which Go needs at once", "candidate": "- `pkg/auth/gateway.go`\n- **Compile floor:** Go requires every method of `SessionGateway` before any implementation compiles\n\n```diff\n+type SessionGateway interface {\n+\tGet(ctx context.Context, key string) (*Session, error)\n+\tPut(ctx context.Context, key string, s *Session, ttl time.Duration) error\n+\tDelete(ctx context.Context, key string) error\n+\tTouch(ctx context.Context, key string, ttl time.Duration) error\n+}\n```", "half": "human", "form": "keep", "note": "Replaces the old stub-at-compile-floor case, which the new contract made impossible (no bodies in Surface, so nothing to stub). A wide interface is pure surface: every line is a signature a caller sees, and the Compile floor bullet is the sanctioned escape from the 150-line per-file cap."}
{"id": "interface-block-script", "intent": "the release check's contract \u2014 the file has no surface to diff", "candidate": "```\nscripts/release-check.sh          # no args, no flags\nexit 0  \u2192 every check passed, one summary line on stdout\nexit 1  \u2192 first failed check on stderr, prefixed `FAIL: `\n```", "half": "human", "form": "keep", "note": "A file whose whole content is a body carries a plain contract block in `## Surface` instead of a ```diff. Human half: it is still the contract the reviewer approves."}
{"id": "body-requested-marker", "intent": "the full Refresh body \u2014 the human asked for it explicitly", "candidate": "- `pkg/auth/handler.go`\n- **Body requested:** `Refresh` \u2014 the human asked for the full body on 2026-08-21\n\n```diff\n+func Refresh(ctx context.Context, req RefreshRequest) (TokenPair, error) {\n+\ts, err := gw.Get(ctx, \"auth:\"+req.Token)\n+\tif err != nil {\n+\t\treturn TokenPair{}, ErrReplayed\n+\t}\n+\tif s.ExpiresAt.Before(time.Now()) {\n+\t\treturn TokenPair{}, ErrExpired\n+\t}\n+\tpair, err := mintTokens(s.UserID)\n+\tif err != nil {\n+\t\treturn TokenPair{}, err\n+\t}\n+\tgw.Delete(ctx, \"auth:\"+req.Token)\n+\treturn pair, nil\n+}\n```", "half": "human", "form": "keep", "note": "The sanctioned exception. Identical content to `go-function-body`, which is `reshape` \u2014 the ONLY difference is the `**Body requested:**` bullet recording that the human asked. Guards both directions: the marker must make a body legal, and its absence must not."}
{"id": "outcome-capability", "intent": "the Outcome for the rotation TODO", "candidate": "A `User` can issue `RotateToken` to exchange a valid refresh token for a new `TokenPair`. On success\nthe `Session` emits `TokenRotated` and the prior refresh token becomes invalid immediately.", "half": "human", "form": "keep", "note": "Capability language, no paths. The anchor of the verification chain."}
{"id": "components-table", "intent": "the symbols this TODO touches", "candidate": "| Component | Touch | Type | Part | Role |\n|-----------|-------|------|------|------|\n| `pkg/auth.Handler` | modify | server | main | Exchanges a valid refresh token for a new pair |\n| `pkg/auth.TokenMinter` | modify | command | supporting | Mints an access/refresh pair for a user id |", "half": "human", "form": "keep", "note": "Names symbols but stays human-half \u2014 it is the last chain link that does, stated without a diff."}
{"id": "autotest-cases-sentences", "intent": "the unit cases for the rotation TODO", "candidate": "- **Target files:** `pkg/auth/handler_test.go` (create)\n- **Cases:**\n  - valid refresh returns a new token pair, both values different from the input\n  - refresh token past its 15-minute TTL returns 401\n  - second refresh with the same token returns 409 and mints nothing\n- **Command:** `go test ./pkg/auth/...`", "half": "human", "form": "keep", "note": "Cases as sentences plus one runnable command \u2014 the prescribed form. Autotest is human-half."}
{"id": "commit-body", "intent": "the commit message the increments build toward", "candidate": "**Title:** `feat: rotate refresh tokens on /auth/refresh`\n\n**Body:**\n\nRefresh tokens stayed valid after use, so one leaked token granted access for as long as the user\nkept refreshing.\n\nEach refresh now returns a new pair and revokes the token it replaces, so a stolen token dies at\nthe next legitimate refresh.", "half": "human", "form": "keep", "note": "Cause + goal paragraphs. The human's last read."}
{"id": "new-terms-row", "intent": "a term this TODO introduces", "candidate": "| Term | Kind | Description |\n|------|------|-------------|\n| TokenJar | entity | Per-user container of active refresh tokens; bounded to 5, LRU-evicted |", "half": "human", "form": "keep", "note": "New terms sits in the human half, right after Outcome."}
{"id": "do-bullet-prose", "intent": "increment 2's instruction \u2014 what to do, no diff", "candidate": "- **Files:** `pkg/auth/token.go`\n- **Surface:** `mintTokens`\n- **Do:** Widen `mintTokens` to the \u00a7 Surface signature. Mint the refresh token alongside the access token and return both; propagate either signing error unchanged. Migrate both call sites to the new return.\n- **Blast radius:** every caller of `mintTokens` \u2014 `pkg/auth/handler.go`, `pkg/auth/login.go`", "half": "agent", "form": "keep", "note": "The canonical increment under the new contract: Files + Surface + Do + Blast radius, no diff. Note it names the call sites to migrate \u2014 a signature change whose callers appear in no Do leaves a broken caller."}
{"id": "behavior-sketch-ts", "intent": "increment 3's Behavior sketch for the rotation flow", "candidate": "- **Behavior:**\n\n```ts\nfunction refresh(req: RefreshRequest): TokenPair | 401 | 409 {\n  const session = redis.get(`auth:${req.token}`)\n  if (!session) return 409 // already rotated \u2014 single-flight\n  if (session.expiresAt < now()) return 401\n\n  const pair = mintTokens(session.userId)\n  redis.del(`auth:${req.token}`)\n  redis.set(`auth:${pair.refresh}`, session, TTL)\n  return pair\n}\n```", "half": "agent", "form": "keep", "note": "A sketch is the prescribed home for logic. Pseudocode nobody can paste \u2014 no real imports, no real paths. Presented with its `- **Behavior:**` bullet: a bare fence let the judge read it as a diff body."}
{"id": "constraints-table", "intent": "the settled decisions the increments below must obey", "candidate": "| Constraint | From |\n|------------|------|\n| A second refresh on the same token returns 409 \u2014 never two valid `TokenPair`s from one token | [[003-decision-single-flight]] |\n| A refresh token expires 15 minutes after issue; the new pair restarts the window | [[001-fact-token-ttl]] |", "half": "agent", "form": "keep", "note": "Moved to the agent half: a constraint is a decision an increment can violate, so it sits above the increments. Guards the one section whose half is counter-intuitive \u2014 it reads like design, but it exists for the implementer."}
{"id": "files-list", "intent": "the agent half's Files section", "candidate": "- `pkg/auth/handler.go` \u2014 modify\n- `pkg/auth/token.go` \u2014 modify\n- `test/e2e/auth_refresh_test.go` \u2014 create", "half": "agent", "form": "keep", "note": "Paths live in the agent half. Tests the half axis on content with no code in it at all."}
{"id": "pre-reads-list", "intent": "the files the implementer must read before editing", "candidate": "- `pkg/auth/middleware.go` \u2014 existing token validation\n- `pkg/redis/client.go` \u2014 Redis helpers used here", "half": "agent", "form": "keep", "note": "Pre-reads is agent-half scaffolding."}
{"id": "manual-test-steps", "intent": "the manual check for the rotation endpoint", "candidate": "- **Steps:**\n  1. `make run-dev`\n  2. `curl -X POST localhost:8080/auth/refresh -d '{\"token\":\"<valid>\"}'`\n- **Expected:**\n  1. dev server starts\n  2. 200 with new `{access, refresh}` pair", "half": "agent", "form": "keep", "note": "Literal commands are the required form here \u2014 a Manual test step is not a body."}
{"id": "sapiens-e2e-script", "intent": "for the Surface block: the end-to-end asset check script", "candidate": "```diff\n+#!/usr/bin/env bash\n+set -euo pipefail\n+\n+REPO=$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)\n+ASSET_ROOT=\"$REPO/sapiens/indexed_model/sapiens\"\n+DESCRIPTOR=\"$REPO/sapiens/dist/tengo/asset/main.as.json\"\n+\n+fail() { echo \"E2E FAIL: $1\" >&2; exit 1; }\n+\n+if [ -n \"${CI:-}\" ]; then\n+    fail \"run with CI unset: pl-pkg publish must not fire from this check\"\n+fi\n+\n+rm -rf \"$REPO/sapiens/indexed_model\" \"$REPO/sapiens/dist\"\n+( cd \"$REPO\" && pnpm build ) 2>&1 | tee \"$REPO/.e2e-build.log\"\n+\n+for variant in vh vl tokenizer; do\n+    while read -r name want; do\n+        out=\"$ASSET_ROOT/$variant/$name\"\n+        [ -f \"$out\" ] || fail \"missing sapiens/$variant/$name\"\n+        got=$(stat -c%s \"$out\" 2>/dev/null || stat -f%z \"$out\")\n+        [ \"$got\" = \"$want\" ] || fail \"sapiens/$variant/$name is $got bytes, manifest declares $want\"\n+    done < <(jq -r '.expected_bytes | to_entries[] | \"\\(.key) \\(.value)\"' \\\n+        \"$REPO/sapiens/$variant.modelInfo.json\")\n+done\n+\n+skipped=$(grep -c \"Already present\" \"$REPO/.e2e-rebuild.log\")\n+[ \"$skipped\" = 8 ] || fail \"expected 8 already-present files, saw $skipped\"\n+\n+echo \"E2E PASS: asset built at $ASSET_ROOT, mountable as 'sapiens', no second fetch\"\n```", "half": "human", "form": "reshape", "note": "THE case this eval exists for \u2014 a real TODO shipped with this pasted as a diff. Every line is implementation: stat flags, jq filters, $REPO paths, the literal 8. Belongs as an Interface block plus a Behavior sketch naming the checks and the edge cases (CI unset, no second fetch, size matches manifest). Offered as Surface content now. Still the case this suite exists for."}
{"id": "go-function-body", "intent": "for the Surface block: the rotation handler", "candidate": "```diff\n+func Refresh(ctx context.Context, req RefreshRequest) (TokenPair, error) {\n+\traw, err := redisClient.Get(ctx, \"auth:\"+req.Token).Result()\n+\tif err == redis.Nil {\n+\t\treturn TokenPair{}, ErrReplayed\n+\t} else if err != nil {\n+\t\treturn TokenPair{}, fmt.Errorf(\"load session: %w\", err)\n+\t}\n+\tvar s Session\n+\tif err := json.Unmarshal([]byte(raw), &s); err != nil {\n+\t\treturn TokenPair{}, fmt.Errorf(\"decode session: %w\", err)\n+\t}\n+\tif s.ExpiresAt.Before(time.Now()) {\n+\t\treturn TokenPair{}, ErrExpired\n+\t}\n+\tpair, err := mintTokens(s.UserID)\n+\tif err != nil {\n+\t\treturn TokenPair{}, err\n+\t}\n+\tredisClient.Del(ctx, \"auth:\"+req.Token)\n+\treturn pair, nil\n+}\n```", "half": "human", "form": "reshape", "note": "Under 150 lines, so no budget catches it. Keep the signature, move the flow to Behavior. A body offered into the Surface diff \u2014 keep the signature, move the flow to the increment's Behavior sketch."}
{"id": "sql-migration-body", "intent": "for the Surface block: the token_jar migration", "candidate": "```diff\n+BEGIN;\n+CREATE TABLE token_jar (\n+  user_id    uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n+  token_hash bytea NOT NULL,\n+  issued_at  timestamptz NOT NULL DEFAULT now(),\n+  PRIMARY KEY (user_id, token_hash)\n+);\n+CREATE INDEX token_jar_issued_idx ON token_jar (user_id, issued_at DESC);\n+DELETE FROM token_jar t WHERE (\n+  SELECT count(*) FROM token_jar o\n+  WHERE o.user_id = t.user_id AND o.issued_at > t.issued_at\n+) >= 5;\n+COMMIT;\n```", "half": "human", "form": "reshape", "note": "Hardest reshape case: the DDL half is arguably schema surface, but the backfill DELETE is a body, so the block as offered must be reshaped. A schema TODO states the columns and the LRU bound; the SQL is the implementer's. Offered as Surface content."}
{"id": "fixture-json-blob", "intent": "for the Surface block: the golden fixture for the rotation test", "candidate": "```diff\n+{\n+  \"sessions\": [\n+    {\"id\": \"0d1c...\", \"userId\": \"u-1\", \"issuedAt\": \"2026-01-01T00:00:00Z\", \"ttl\": 900},\n+    {\"id\": \"8ab2...\", \"userId\": \"u-1\", \"issuedAt\": \"2026-01-01T00:05:00Z\", \"ttl\": 900},\n+    {\"id\": \"f31e...\", \"userId\": \"u-2\", \"issuedAt\": \"2026-01-01T00:07:00Z\", \"ttl\": 900}\n+  ],\n+  \"expected\": {\"u-1\": 2, \"u-2\": 1}\n+}\n```", "half": "human", "form": "reshape", "note": "A fixture is named as a body explicitly. What the TODO owes is the shape and the edge case (two sessions for one user), not the literals. Offered as Surface content."}
{"id": "parse-loop-regex", "intent": "for the Surface block: parsing the manifest lines", "candidate": "```diff\n+var lineRe = regexp.MustCompile(`^(\\S+)\\s+(\\d+)$`)\n+\n+for _, ln := range strings.Split(string(raw), \"\\n\") {\n+\tm := lineRe.FindStringSubmatch(strings.TrimSpace(ln))\n+\tif m == nil {\n+\t\tcontinue\n+\t}\n+\tn, err := strconv.Atoi(m[2])\n+\tif err != nil {\n+\t\treturn nil, fmt.Errorf(\"bad size on %q: %w\", ln, err)\n+\t}\n+\tout[m[1]] = n\n+}\n```", "half": "human", "form": "reshape", "note": "A regex and a loop, both named as bodies. Not even a signature here \u2014 the increment has nothing keepable as offered. Offered as Surface content."}
{"id": "do-bullet-with-code", "intent": "increment 3's instruction, with the code spelled out to be safe", "candidate": "- **Files:** `pkg/auth/handler.go`\n- **Surface:** `Refresh`\n- **Do:** Reshape `Refresh` and implement the rotation like this:\n\n```go\ns, err := gw.Get(ctx, \"auth:\"+req.Token)\nif err != nil {\n\treturn TokenPair{}, ErrReplayed\n}\npair, _ := mintTokens(s.UserID)\ngw.Delete(ctx, \"auth:\"+req.Token)\nreturn pair, nil\n```\n- **Blast radius:** every caller of `Refresh`", "half": "agent", "form": "reshape", "note": "Right half, wrong form \u2014 the agent half is where increments live, but a Do is prose. Real code inside a Do is a body twice over: it belongs to no diff and it duplicates what Surface plus the Behavior sketch already carry."}
{"id": "autotest-go-test-source", "intent": "the unit test for the rotation TODO", "candidate": "- **Target files:** `pkg/auth/handler_test.go` (create)\n- **Cases:**\n\n```go\nfunc TestRefresh_Rotates(t *testing.T) {\n\ts := seedSession(t, \"u-1\", 15*time.Minute)\n\tpair, err := Refresh(ctx, RefreshRequest{Token: s.Refresh})\n\trequire.NoError(t, err)\n\trequire.NotEqual(t, s.Refresh, pair.Refresh)\n\t_, err = redisClient.Get(ctx, \"auth:\"+s.Refresh).Result()\n\trequire.ErrorIs(t, err, redis.Nil)\n}\n```\n- **Command:** `go test ./pkg/auth/...`", "half": "human", "form": "reshape", "note": "Right half, wrong form \u2014 Autotest is human-half, but a test file's assertions are a body. The Cases must become sentences."}
{"id": "autotest-expected-bytes-table", "intent": "the E2E cases for the asset build", "candidate": "- **Target files:** `test/e2e/assets_test.sh` (create)\n- **Cases:**\n\n| file | expected bytes |\n|------|----------------|\n| vh/model.safetensors | 421337088 |\n| vh/config.json | 1042 |\n| vl/model.safetensors | 168820736 |\n| tokenizer/tokenizer.json | 2113682 |\n- **Command:** `./test/e2e/assets_test.sh`", "half": "human", "form": "reshape", "note": "A table of literal expected values is named as a body. The case is 'every file matches the size its manifest declares' \u2014 the numbers live in the manifest, not the TODO."}
