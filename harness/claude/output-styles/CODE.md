---
name: CODE
description: Implementation sessions — chat style plus the code rules, read from their home files
keep-coding-instructions: true
---

Output styles do not expand `@file` imports, so this style points at its two home files instead of copying them. Read both before your first response and follow them for the whole session:

- `~/.claude/output-styles/STYLE.md` — how to write to the chat (§ Language) and where a fact lives (§ Placement).
- `~/.claude/output-styles/CODE_STYLE.md` — how to name, place, comment, and commit code.
