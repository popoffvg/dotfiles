{
  "name": "local-plugins",
  "owner": {
    "name": "popoffvg"
  },
  "plugins": [
    {
      "name": "harness-dev",
      "source": "./harness/plugins/harness-dev",
      "description": "Authoring aids for the Claude Code harness — skill shapes (workflow, loop, instruction, router), pruning an existing skill or doc corpus into the house style, and repo bootstrap (README, CLAUDE.md, deploy and adr skills, settings).",
      "version": "0.10.0"
    },
    {
      "name": "hi",
      "source": "./harness/plugins/hi",
      "description": "Per-session prompt stack: push/pop/list/drop prompts across a session.",
      "version": "0.3.0"
    },
    {
      "name": "line-comment",
      "source": "./harness/plugins/line-comment",
      "description": "Review comments on lines of any file, kept outside the file. Zed writes them through the line-comment language server; /line-comment:act reads the store and does the work, /line-comment:write places comments, /line-comment:unresolved reports what is still open, /line-comment:prune throws comments away.",
      "version": "0.4.0"
    },
    {
      "name": "rules",
      "source": "./harness/plugins/rules",
      "description": "Narrow code rules, one trigger and one rule each, scoped to the file type they govern. A rule lands here once it is worth obeying on every matching file rather than remembering from the session that taught it. The self-improvement plugin captures and consolidates lessons; this plugin is where the surviving ones live.",
      "version": "0.2.0"
    },
    {
      "name": "self-improvement",
      "source": "./harness/plugins/self-improvement",
      "description": "Scores every Claude Code session for the lessons it holds and proposes where each one belongs in the harness: a watermarked background scan kicked by the SessionStart hook, one haiku call per dirty session, then a suggestion pass over the existing skill inventory. Browsable in the self-improve TUI. High scorers are archived for the capture-lesson skill to turn into skills and the dream skill to consolidate.",
      "version": "0.28.0"
    },
    {
      "name": "smart-commit",
      "source": "./harness/plugins/smart-commit",
      "description": "Split the working tree of every nearby git repo into logical commits at hunk granularity. A sonnet subagent proposes the split into an editable file, the operator answers each block, a second subagent builds the commits in a throwaway worktree and cherry-picks them back.",
      "version": "0.2.0"
    },
    {
      "name": "wm",
      "source": "./harness/plugins/wm",
      "description": "Work lifecycle management v2 — phased workflows (research, plan, implement) with agents, commands, and hooks.",
      "version": "2.63.0"
    }
  ]
}
