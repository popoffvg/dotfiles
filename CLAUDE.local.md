# CLAUDE.local.md

All captured rules now live as skills in `~/.claude/skills/`, each stamped `metadata.origin: self-improvement`. New lessons go there too — see the `capture-lesson` skill (`self-improvement` plugin).
