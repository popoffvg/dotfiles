pre-commit:
  parallel: true
  commands:
    gitleaks:
      run: gitleaks protect --staged --no-banner
    bump-plugin-version:
      glob: "harness/plugins/*/**"
      run: bash harness/scripts/bump-plugin-version.sh
